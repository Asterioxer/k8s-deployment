# frontend/Dockerfile
FROM nginx:stable-alpine
COPY index.html /usr/share/nginx/html/index.html
# simple proxy pass not used — we use NodePort/Ingress to route requests
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
